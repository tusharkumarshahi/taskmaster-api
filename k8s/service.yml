apiVersion: v1
kind: Service
metadata:
  name: taskmaster-api
  namespace: taskmaster
  labels:
    app: taskmaster-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: taskmaster-api